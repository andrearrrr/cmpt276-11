<% provide(:title, @title) %>
  <div class="col-md-8">
    <h5><%= link_to "Go Back", user_path(@user) %></h5>
    <h3><%= @title %></h3>
    <% if @users.any? %>
      <ul class="users follow">
        <%= render @users %>
      </ul>
    <% end %>
  </div>
</div>
