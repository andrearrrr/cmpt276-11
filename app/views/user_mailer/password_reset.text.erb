Hi <%= @user.name %>,

We have received a request on MySportsPicks to reset your password.

To reset your password, follow the link below:
<%= edit_password_reset_url(@user.reset_token, email: @user.email) >


Visit the link within 2 hours before it expires

If the password is not changed within 2 hours, it will remain the same.
