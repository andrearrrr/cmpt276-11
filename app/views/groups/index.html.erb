<% provide(:title, 'Groups') %>
<p id="notice"><%= notice %></p>
<h1> Groups </h1>

<ul class="users">
  <% @groups.each do |t| %>
  <li>
    <%= link_to t.name, group_path(t) %>
  </li>
  <% end %>
</ul>
